#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main () {
        int     fd[2];
        pid_t   pid_child;
		
        // establish the pipe 
		
		// create a child process	  
		
		// the child
      
        // changed the standard outlet for the pipe 
		
        // executes the writer program  that will receive the standard output, which will be the pipe 
			
		// exit if there is an error
		
        // the parent 
		
        // changed the standard input for the pipe 
		
		// executes the reader program  that will receive the standard input, which will be the pipe 

 		// exit if there is an error

}
